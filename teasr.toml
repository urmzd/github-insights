[server]
command = "npx serve metrics --listen 3123"
url = "http://localhost:3123"
timeout = 10000

[output]
dir = "./showcase"
formats = ["png"]

[[scenes]]
type = "web"
url = "/index.svg"
name = "metrics-svg"
viewport = { width = 1200, height = 900 }
